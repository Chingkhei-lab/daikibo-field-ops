import { Activity } from '../types';

// Placeholder for conflict resolution logic
export const resolveConflict = (local: Activity, _remote: any): Activity => {
    // Logic to merge data
    // For now, return local or remote based on timestamp
    return local;
};
